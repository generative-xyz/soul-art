export enum MediaType {
  IMAGE = 'IMAGE',
  MODEL_3D = 'MODEL_3D',
  VIDEO = 'VIDEO',
  AUDIO = 'AUDIO',
  IFRAME = 'IFRAME',
  TEXT = 'TEXT',
  PDF = 'PDF',
}

export enum ChunkProcessStatus {
  New = 0,
  Processing = 1,
  Done = 2,
}

export enum FileProcessStatus {
  New = 0,
  Processing = 1,
  Done = 2,
}
